import React from "react";

const Home = () => {
  return (
    <div className="homeDiv">
      <h1>Welcome!</h1>
      <p>
        Widget App is an aplication that allows users to view the current time,
        and weather in there current location. The user can navigate between the
        weather and the time via the navigation bar on the left. Also don't
        forget to try the Dark Mode feature!
      </p>
    </div>
  );
};

export default Home;
